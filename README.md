A browser-based app that detects 468 facial landmarks in real-time from your webcam using MediaPipe and Streamlit WebRTC.
Tech Stack: Streamlit (UI) · streamlit-webrtc (webcam) · MediaPipe (face detection) · OpenCV (image processing)
Run locally: pip install -r requirements.txt then streamlit run app.py
Deployed on: Streamlit Cloud